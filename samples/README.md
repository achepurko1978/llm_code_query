# Samples

This folder contains:
- `cpp/`: C++ source files used as test inputs
- `requests/`: JSON request samples for `clang_mcp.py`
- `responses/`: JSON outputs generated by running those requests

## Re-run all sample tests

```bash
bash samples/run_samples.sh
```

The script rebuilds with clang and regenerates all response JSON files.
