version: "3.9"

services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: llm-code-query-dev
    working_dir: /workspace
    volumes:
      - ./:/workspace
    stdin_open: true
    tty: true
